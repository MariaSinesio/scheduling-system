<% if user_signed_in? %>

<h1>Welcome, <%= current_user.name %></h1>

<%= button_to "Sign out", destroy_user_session_path, method: :delete  %> 

<%# Tive que trocar para um button, simplesmentenão estava funcionando o link_to. Por que? Ninguem sabe. %>

<%# <% else %>

<%# data: { turbo_method: ":delete" } %> 

<%# method: :delete %>

<%end%>

<h1>Agende sua vacinação</h1>

<div>
<h2>Febre amarela</h2>
</div>